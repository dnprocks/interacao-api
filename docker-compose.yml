version: "3.9"

services:
  interacao-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: interacao-api-container
    restart: always
    ports:
      - "3009:3009"
    environment:
      NODE_ENV: production
      PORT: 3009
