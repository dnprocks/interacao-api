version: "3.9"

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: interacao-api
    restart: always
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
